<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coding404.myweb.topic.TopicMapper">
    <insert id="topicRegist" parameterType="TopicVO">
        INSERT INTO TOPIC(TOPIC_REGDATE, TOPIC_WRITER, TOPIC_TITLE, TOPIC_CONTENT)
        VALUES (#{topicRegdate}, #{topicWriter}, #{topicTitle}, #{topicContent})
    </insert>

    <select id="topicListAll" resultType="TopicVO">
        SELECT * FROM TOPIC
        <where>
            <if test="searchName != '' and searchName != null">
                AND TOPIC_TITLE LIKE CONCAT('%', #{searchName}, '%')
            </if>
            <if test="searchWriter != '' and searchWriter != null">
                AND TOPIC_WRITER LIKE CONCAT('%', #{searchWriter}, '%')
            </if>
        </where>
        ORDER BY TOPIC_REGDATE DESC
        LIMIT #{pageStart}, #{amount} </select>

    <select id="getTotal" resultType="int">
        SELECT COUNT(*) AS total FROM TOPIC
        <where>
            <if test="searchName != '' and searchName != null">
                AND TOPIC_TITLE LIKE CONCAT('%', #{searchName}, '%')
            </if>
            <if test="searchWriter != '' and searchWriter != null">
                AND TOPIC_WRITER LIKE CONCAT('%', #{searchWriter}, '%')
            </if>
        </where>
    </select>

    <select id="getTotalMe" resultType="int" parameterType="string">
        SELECT COUNT(*) AS total FROM TOPIC
        WHERE TOPIC_WRITER = #{topicWriter}
    </select>

    <select id="topicListMe" resultType="TopicVO">
        SELECT * FROM TOPIC
        WHERE TOPIC_WRITER = #{topicWriter}
        ORDER BY TOPIC_REGDATE DESC
            LIMIT #{cri.pageStart}, #{cri.amount}
    </select>

    <select id="topicDetail" resultType="TopicVO" parameterType="long">
        SELECT * FROM TOPIC
        WHERE TOPIC_ID = #{topicId}
    </select>

    <update id="topicModify" parameterType="TopicVO">
        UPDATE TOPIC
        SET TOPIC_WRITER = #{topicWriter},
            TOPIC_TITLE = #{topicTitle}, TOPIC_CONTENT = #{topicContent}
        WHERE TOPIC_ID = #{topicId}
    </update>

    <delete id="topicDelete" parameterType="long">
        DELETE FROM TOPIC
        WHERE TOPIC_ID = #{topicId}
    </delete>
</mapper>